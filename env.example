# Metasploit RPC Configuration
MSF_PASSWORD=your_secure_password_here
DB_PASSWORD=your_database_password_here

# Server Configuration
MCP_PORT=8085
LOG_LEVEL=INFO

# Optional: Domain for SSL (if using nginx with SSL)
DOMAIN=your-domain.com

# Security: Add strong passwords before deployment!
# Generate secure passwords with: openssl rand -base64 32 